#include <stdio.h>
#include "../include/utils.h"

int main() {
    // Example usage of utility functions
    int result = add(5, 3); // Assuming add is a function declared in utils.h
    printf("The result of adding 5 and 3 is: %d\n", result);

    return 0;
}